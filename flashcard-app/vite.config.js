import { defineConfig } from 'vite';

// Adjust `base` to your repository name for GitHub Pages project sites.
// If deploying to <username>.github.io (user/organization site), change to '/'.
export default defineConfig({
  base: '/thai-study/',
});

